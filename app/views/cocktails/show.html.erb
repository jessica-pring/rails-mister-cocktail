<h2><%= @cocktail.name %></h2>

<h4>Doses</h4>
<ul>
 <% @cocktail.doses.each do |dose| %>
  <li><%= dose.description %> <%= dose.ingredient.name %></li>
 <% end %>
</ul>

<p><%= link_to 'Add a new dose', new_cocktail_dose_path(@cocktail) %></p>
<p><%= link_to 'Go back to home', cocktails_path %></p>